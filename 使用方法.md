# DDP Baseline 使用方法

## 环境

需要 Python 3.10+ 和 PyTorch 2.1+。安装依赖：

```bash
python -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
```

## CUDA 真实显卡运行

服务器分工：101 用于 Agent 辅助开发和 CPU 验证，200 用于真实 CUDA 实验。先在 101 验证代码，再同步到 200；不要把 101 的 CPU 结果当作 GPU 基线。

先确认当前 shell 能看到显卡：

```bash
nvidia-smi
python -c "import torch; print(torch.cuda.is_available(), torch.cuda.device_count())"
```

使用 `torchrun` 为每张 GPU 启动一个进程。下面命令会使用真实 CUDA 设备、NCCL 和 CUDA AMP：

```bash
torchrun --standalone --nproc_per_node=2 -m ddp_baseline.train \
  --device cuda --amp --epochs 5 --batch-size 64 --checkpoint-every-steps 100 \
  --checkpoint-dir runs/cuda
```

`--nproc_per_node` 不应超过当前可见 GPU 数量。单卡运行时改为 `--nproc_per_node=1`。

## 测试与 CPU 调试

```bash
python3 -m unittest discover -s tests -v
torchrun --standalone --nproc_per_node=2 -m ddp_baseline.train \
  --device cpu --epochs 2 --samples 512 --batch-size 16 \
  --checkpoint-dir runs/cpu
```

CPU 命令只验证控制流和 checkpoint，不代表真实 GPU 性能。单进程 CPU 调试：

```bash
python -m ddp_baseline.train --device cpu --epochs 2 --checkpoint-dir runs/debug
```

## 梯度累积与恢复

```bash
torchrun --standalone --nproc_per_node=2 -m ddp_baseline.train \
  --device cuda --amp --epochs 5 \
  --batch-size 64 --accumulation-steps 4 --checkpoint-every-steps 50 \
  --checkpoint-dir runs/cuda-accum
torchrun --standalone --nproc_per_node=2 -m ddp_baseline.train \
  --device cuda --amp --epochs 8 --checkpoint-dir runs/cuda \
  --resume runs/cuda/checkpoint.pt
```

无 GPU 时才使用 CPU 梯度累积调试：

```bash
python3 -m ddp_baseline.train --device cpu --epochs 5 \
  --batch-size 16 --accumulation-steps 4 --checkpoint-dir runs/accum
```

训练每完成指定数量的 optimizer step 就保存一次 checkpoint。`checkpoint.pt` 始终是最新状态，`checkpoint-step-XXXXXXXX.pt` 是带 step 编号的快照。恢复会读取 epoch、当前 epoch 内的 batch、global step、samples_seen、模型、优化器、scheduler、AMP scaler 和各 rank RNG，并从中断位置继续。

训练使用 step 级 warmup/cosine 学习率调度器，可通过 `--warmup-steps` 设置 warmup 步数。resume 前会校验模型、数据、batch、累积步数、精度、seed 和 world size 等关键配置；修改这些配置后不能直接当作无缝续训。

## 输出

每个实验目录包含 `config.json`、`metrics.jsonl`、最新的 `checkpoint.pt` 和可选的 step 快照。指标 JSONL 记录 epoch、全局平均 loss、samples/s 和 world size。多机训练、NPU/HCCL、Profiler、FSDP/ZeRO 不在本项目范围内。
