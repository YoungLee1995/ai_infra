已定位，原因不是 SSH 跳板，而是 200 的出站网络策略。
证据：
- 101 → 200：正常，Ping 0% 丢包，TCP/22 可用。
- 200 默认网关 10.28.80.254 正常，1.1.1.1:443 可用。
- 200 访问以下 HTTPS 均失败：
  - easymax.ai:443：立即 Connection refused
  - api.openai.com:443：立即 Connection refused
  - GitHub：超时
- 101 访问真实 Codex 配置中的 https://easymax.ai/v1 返回 HTTP 404，说明 101 出口可达。
- 200 的 Codex 配置使用自定义服务：[config.toml](/home/liard/.codex/config.toml)，不是直连 OpenAI。
- 临时通过 SSH 借用 101 出口访问 easymax.ai 成功，返回 HTTP 404，进一步证明 200 的 80 网段出口被拦截。
另外，200 上的：
- 8080 是 Docker 映射的 SSH 端口，不是代理；
- 8100 是需要认证的代理服务，当前用户无法直接使用。
建议处理方式：
1. 让网络管理员允许 10.28.80.200 出站 TCP/443，至少放行 easymax.ai 及其 DNS 返回的地址。
2. 如果网络要求代理，获取正确的代理地址和认证信息，然后配置到 VS Code Remote/Codex 进程环境中。
3. 临时方案是让 200 通过 101 做 SOCKS/HTTP 出口，但需要在 101 上配置转发并让 Codex 使用该代理。
当前没有修改任何系统或 Codex 配置。

刚才的结论需要修正：
- 200 上通过 GitHub HTTPS 下载是可以的，已验证：
  git ls-remote https://github.com/git/git.git HEAD 成功。
- GitHub SSH 的 22 端口也能连通，但当前没有配置 GitHub SSH 公钥，所以返回 Permission denied (publickey)。
- 普通 curl https://github.com 曾超时，但不代表 Git HTTPS 一定不可用，Git 的实际请求已经成功。
因此，200 可以通过 HTTPS 下载部分公开 GitHub 项目；但 Codex 使用的 easymax.ai 仍然无法访问，这才是当前 Codex 不能工作的直接原因